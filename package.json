{
  "name": "inmova",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "db:init": "pnpm -r run db:init",
    "db:seed": "pnpm -r run db:seed"
  }
}